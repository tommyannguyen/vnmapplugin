# VnMapPlugin — Full Documentation Refactor & Cleanup — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebrand all user-facing documentation and source files from `@vnmapplugin/maps`/Mapbox to `@vnmapplugin/maps`/VnMapPlugin throughout the repository.

**Architecture:** Five phases — delete legacy files, global search-replace passes for package names and brand terms (ordered to avoid partial matches), targeted manual edits for files needing structural fixes, autogenerated template fix, and a final grep review pass.

**Tech Stack:** PowerShell, Git, TypeScript, EJS templates, Markdown

---

## Files Overview

| File | Change Type |
|------|------------|
| `CHANGELOG.md` | Delete |
| `README-v10.md` | Delete |
| All `.md` files in repo (excl. node_modules/lib) | Global replace + typo fixes |
| `scripts/templates/component.md.ejs` | Global replace + targeted fix (lines 13, 93) |
| `android/install.md` | Global replace + typo fixes |
| `ios/install.md` | Global replace + rewrite opening |
| `docs/VnMapPlugin.md` | Global replace + heading + typo + table fix |
| `CONTRIBUTING.md` | Global replace + typo fix |
| `RELEASE.md` | Global replace |
| `src/vmapPlugin.native.ts` | StyleURL enum values |
| All `.ts`/`.tsx`/`.js`/`.mjs`/`.ejs`/`.json`/`.yml`/`.gradle`/`.rb` | Global replace (package names + native vars) |

---

### Task 1: Delete legacy files

**Files:**
- Delete: `CHANGELOG.md`
- Delete: `README-v10.md`

- [ ] **Step 1: Delete the files**

```powershell
Remove-Item "C:\Nca\vnmapplugin\CHANGELOG.md"
Remove-Item "C:\Nca\vnmapplugin\README-v10.md"
```

- [ ] **Step 2: Verify deletion**

```powershell
Test-Path "C:\Nca\vnmapplugin\CHANGELOG.md"
Test-Path "C:\Nca\vnmapplugin\README-v10.md"
```

Expected: `False` for both.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "chore: remove legacy CHANGELOG.md and README-v10.md"
```

---

### Task 2: Global replace — `@vnmapplugin/maps-example`

**Files:** All `.md`, `.ts`, `.tsx`, `.js`, `.mjs`, `.json`, `.yml`, `.yaml` (excluding `node_modules`, `.git`, `lib`, `.yarn`)

Note: Replace `-example` variant BEFORE the base package name to avoid partial-match corruption.

- [ ] **Step 1: Run the replacement**

```powershell
$root = "C:\Nca\vnmapplugin"
$exts = @('.md','.ts','.tsx','.js','.mjs','.ejs','.json','.yml','.yaml')
$excl = 'node_modules|\\\.git\\|\\lib\\|\\\.yarn\\'
Get-ChildItem -Path $root -Recurse -File |
  Where-Object { $exts -contains $_.Extension -and $_.FullName -notmatch $excl } |
  ForEach-Object {
    $c = [System.IO.File]::ReadAllText($_.FullName, [Text.Encoding]::UTF8)
    if ($c.Contains('@vnmapplugin/maps-example')) {
      [System.IO.File]::WriteAllText($_.FullName, $c.Replace('@vnmapplugin/maps-example','@vnmapplugin/maps-example'), [Text.Encoding]::UTF8)
      Write-Host "Updated: $($_.Name)"
    }
  }
```

- [ ] **Step 2: Verify — no remaining occurrences**

```powershell
Select-String -Path "C:\Nca\vnmapplugin" -Pattern "@vnmapplugin/maps-example" -Recurse `
  -Include "*.md","*.ts","*.tsx","*.js","*.json","*.yml" |
  Where-Object { $_.Path -notmatch 'node_modules|\\lib\\|\.git|\.yarn' }
```

Expected: No output.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "chore: replace @vnmapplugin/maps-example with @vnmapplugin/maps-example"
```

---

### Task 3: Global replace — `@vnmapplugin/maps`

**Files:** Same scope as Task 2.

- [ ] **Step 1: Run the replacement**

```powershell
$root = "C:\Nca\vnmapplugin"
$exts = @('.md','.ts','.tsx','.js','.mjs','.ejs','.json','.yml','.yaml')
$excl = 'node_modules|\\\.git\\|\\lib\\|\\\.yarn\\'
Get-ChildItem -Path $root -Recurse -File |
  Where-Object { $exts -contains $_.Extension -and $_.FullName -notmatch $excl } |
  ForEach-Object {
    $c = [System.IO.File]::ReadAllText($_.FullName, [Text.Encoding]::UTF8)
    if ($c.Contains('@vnmapplugin/maps')) {
      [System.IO.File]::WriteAllText($_.FullName, $c.Replace('@vnmapplugin/maps','@vnmapplugin/maps'), [Text.Encoding]::UTF8)
      Write-Host "Updated: $($_.Name)"
    }
  }
```

- [ ] **Step 2: Verify**

```powershell
Select-String -Path "C:\Nca\vnmapplugin" -Pattern "@vnmapplugin/maps" -Recurse `
  -Include "*.md","*.ts","*.tsx","*.js","*.json","*.yml" |
  Where-Object { $_.Path -notmatch 'node_modules|\\lib\\|\.git|\.yarn' }
```

Expected: No output.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "chore: replace @vnmapplugin/maps package name with @vnmapplugin/maps"
```

---

### Task 4: Global replace — GitHub repo path `rnmapbox/maps`

**Files:** All `.md`, `.ts`, `.tsx`, `.js`, `.mjs`, `.yml`, `.yaml`, `.ejs` (excluding `node_modules`, `.git`, `lib`, `.yarn`)

- [ ] **Step 1: Run the replacement**

```powershell
$root = "C:\Nca\vnmapplugin"
$exts = @('.md','.ts','.tsx','.js','.mjs','.ejs','.yml','.yaml')
$excl = 'node_modules|\\\.git\\|\\lib\\|\\\.yarn\\'
Get-ChildItem -Path $root -Recurse -File |
  Where-Object { $exts -contains $_.Extension -and $_.FullName -notmatch $excl } |
  ForEach-Object {
    $c = [System.IO.File]::ReadAllText($_.FullName, [Text.Encoding]::UTF8)
    if ($c.Contains('rnmapbox/maps')) {
      [System.IO.File]::WriteAllText($_.FullName, $c.Replace('rnmapbox/maps','tommyannguyen/vnmapplugin'), [Text.Encoding]::UTF8)
      Write-Host "Updated: $($_.Name)"
    }
  }
```

- [ ] **Step 2: Verify**

```powershell
Select-String -Path "C:\Nca\vnmapplugin" -Pattern "rnmapbox/maps" -Recurse `
  -Include "*.md","*.ts","*.tsx","*.js","*.yml","*.ejs" |
  Where-Object { $_.Path -notmatch 'node_modules|\\lib\\|\.git|\.yarn' }
```

Expected: No output.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "chore: update GitHub repo paths from rnmapbox/maps to tommyannguyen/vnmapplugin"
```

---

### Task 5: Global replace — native variable prefix `RNMapboxMaps`

**Files:** All `.md`, `.ts`, `.tsx`, `.js`, `.mjs`, `.ejs`, `.kt`, `.gradle`, `.rb`, `.podspec`, `.swift`, `.yml` (excluding `node_modules`, `.git`, `lib`, `.yarn`)

This replaces `RNMapboxMaps` (and all its suffixed variants: `RNMapboxMapsVersion`, `RNMapboxMapsImpl`, `RNMapboxMapsLibs`) in a single pass.

- [ ] **Step 1: Run the replacement**

```powershell
$root = "C:\Nca\vnmapplugin"
$exts = @('.md','.ts','.tsx','.js','.mjs','.ejs','.kt','.gradle','.rb','.podspec','.swift','.yml')
$excl = 'node_modules|\\\.git\\|\\lib\\|\\\.yarn\\'
Get-ChildItem -Path $root -Recurse -File |
  Where-Object { $exts -contains $_.Extension -and $_.FullName -notmatch $excl } |
  ForEach-Object {
    $c = [System.IO.File]::ReadAllText($_.FullName, [Text.Encoding]::UTF8)
    if ($c.Contains('RNMapboxMaps')) {
      [System.IO.File]::WriteAllText($_.FullName, $c.Replace('RNMapboxMaps','RNVnMapMaps'), [Text.Encoding]::UTF8)
      Write-Host "Updated: $($_.Name)"
    }
  }
```

- [ ] **Step 2: Verify**

```powershell
Select-String -Path "C:\Nca\vnmapplugin" -Pattern "RNMapboxMaps" -Recurse `
  -Include "*.md","*.ts","*.js","*.kt","*.gradle","*.rb","*.yml" |
  Where-Object { $_.Path -notmatch 'node_modules|\\lib\\|\.git|\.yarn' }
```

Expected: No output.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "chore: rename RNMapboxMaps native variable prefix to RNVnMapMaps"
```

---

### Task 6: Global replace — `MapboxGL` brand name (markdown only)

**Files:** All `.md` files only (keeping `.ts`/`.tsx` identifiers like `MapboxGLEvent` untouched — they are internal types, not user-facing names).

- [ ] **Step 1: Run the replacement on .md files**

```powershell
$root = "C:\Nca\vnmapplugin"
$excl = 'node_modules|\\\.git\\|\\lib\\|\\\.yarn\\'
Get-ChildItem -Path $root -Recurse -File -Filter "*.md" |
  Where-Object { $_.FullName -notmatch $excl } |
  ForEach-Object {
    $c = [System.IO.File]::ReadAllText($_.FullName, [Text.Encoding]::UTF8)
    if ($c.Contains('MapboxGL')) {
      [System.IO.File]::WriteAllText($_.FullName, $c.Replace('MapboxGL','VnMapPlugin'), [Text.Encoding]::UTF8)
      Write-Host "Updated: $($_.Name)"
    }
  }
```

- [ ] **Step 2: Verify**

```powershell
Select-String -Path "C:\Nca\vnmapplugin" -Pattern "MapboxGL" -Recurse -Include "*.md" |
  Where-Object { $_.Path -notmatch 'node_modules|\\lib\\|\.git|\.yarn' }
```

Expected: No output.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "chore: replace MapboxGL brand name with VnMapPlugin in docs"
```

---

### Task 7: Global replace — `Mapbox` brand name (markdown only)

**Files:** All `.md` files only. Run after `MapboxGL` replacement (Task 6) to avoid double-replace.

- [ ] **Step 1: Run the replacement on .md files**

```powershell
$root = "C:\Nca\vnmapplugin"
$excl = 'node_modules|\\\.git\\|\\lib\\|\\\.yarn\\'
Get-ChildItem -Path $root -Recurse -File -Filter "*.md" |
  Where-Object { $_.FullName -notmatch $excl } |
  ForEach-Object {
    $c = [System.IO.File]::ReadAllText($_.FullName, [Text.Encoding]::UTF8)
    if ($c.Contains('Mapbox')) {
      [System.IO.File]::WriteAllText($_.FullName, $c.Replace('Mapbox','VnMapPlugin'), [Text.Encoding]::UTF8)
      Write-Host "Updated: $($_.Name)"
    }
  }
```

- [ ] **Step 2: Verify**

```powershell
Select-String -Path "C:\Nca\vnmapplugin" -Pattern "Mapbox" -Recurse -Include "*.md" |
  Where-Object { $_.Path -notmatch 'node_modules|\\lib\\|\.git|\.yarn' }
```

Expected: No output (all occurrences in .md files replaced).

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "chore: replace Mapbox brand name with VnMapPlugin in docs"
```

---

### Task 8: Fix `docs/VnMapPlugin.md` — heading, typo, broken method table

**Files:**
- Modify: `docs/VnMapPlugin.md`

- [ ] **Step 1: Fix heading — line 1**

Change:
```markdown
# Mapbox
```
To:
```markdown
# VnMapPlugin
```

(Note: After Task 7, this will already read `# VnMapPlugin` — verify it, skip if already correct.)

- [ ] **Step 2: Fix typo in `setWellKnownTileServer` description**

Find:
```markdown
#### Description
Deprecard will be removed on next version
```
Replace with:
```markdown
#### Description
Deprecated — will be removed in the next version.
```

- [ ] **Step 3: Fix `getAccessToken()` — remove incorrect argument table**

The method takes no arguments. Find and replace this entire block:

```markdown
### getAccessToken()

#### arguments
| Name | Type | Required | Description  |
| ---- | :--: | :------: | :----------: |
| `accessToken` | `String` | `Yes` | required acessToken to pull mapbox tiles, can be `null` if other tiles are used |

#### Description
gets the accessToken
```

With:

```markdown
### getAccessToken()

#### Description
Returns the current access token.
```

- [ ] **Step 4: Verify the heading is correct**

```powershell
(Get-Content "C:\Nca\vnmapplugin\docs\VnMapPlugin.md")[0]
```

Expected: `# VnMapPlugin`

- [ ] **Step 5: Commit**

```bash
git add docs/VnMapPlugin.md
git commit -m "fix: correct VnMapPlugin.md heading, deprecated typo, and getAccessToken signature"
```

---

### Task 9: Rewrite `ios/install.md` — fix incomplete opening

**Files:**
- Modify: `ios/install.md`

- [ ] **Step 1: Fix the incomplete opening (lines 1–5)**

Current content:
```markdown
# iOS Installation


`@vnmapplugin/maps` via `npm` or `yarn`.
```

Replace with:
```markdown
# iOS Installation

Install `@vnmapplugin/maps` via `npm` or `yarn` before following this guide.
```

- [ ] **Step 2: Verify the pre/post install hook variable names are correct**

After Task 5 replaced `RNMapboxMaps` → `RNVnMapMaps`, verify these lines read:
```ruby
    $RNVnMapMaps.pre_install(installer)
```
and:
```ruby
    $RNVnMapMaps.post_install(installer)
```

```powershell
Select-String -Path "C:\Nca\vnmapplugin\ios\install.md" -Pattern "RNVnMapMaps|RNMapboxMaps"
```

Expected: Only `RNVnMapMaps` occurrences, no `RNMapboxMaps`.

- [ ] **Step 3: Verify the version variable reference is correct**

```powershell
Select-String -Path "C:\Nca\vnmapplugin\ios\install.md" -Pattern "RNVnMapMapsVersion|RNMapboxMapsVersion"
```

Expected: Only `RNVnMapMapsVersion`, no `RNMapboxMapsVersion`.

- [ ] **Step 4: Commit**

```bash
git add ios/install.md
git commit -m "fix: rewrite ios/install.md opening and verify branding"
```

---

### Task 10: Fix `android/install.md` — typos

**Files:**
- Modify: `android/install.md`

- [ ] **Step 1: Fix "updatign" typo**

Find:
```
we recommend updatign to 11.*
```
Replace with:
```
we recommend updating to 11.*
```

- [ ] **Step 2: Fix "exepect" typo**

Find:
```
will likely result in a build error
```
(Search for `exepect` — it appears in: `Setting it to earlier version than what we exepect will likely result in a build error.`)

Replace `exepect` with `expect`.

- [ ] **Step 3: Fix "neccesary" typo**

Find:
```
should it be neccesary
```
Replace with:
```
should it be necessary
```

- [ ] **Step 4: Verify no remaining typos or old branding**

```powershell
Select-String -Path "C:\Nca\vnmapplugin\android\install.md" `
  -Pattern "updatign|exepect|neccesary|RNMapboxMaps|@rnmapbox"
```

Expected: No output.

- [ ] **Step 5: Commit**

```bash
git add android/install.md
git commit -m "fix: correct typos in android/install.md"
```

---

### Task 11: Update `StyleURL` enum in `src/vmapPlugin.native.ts`

**Files:**
- Modify: `src/vmapPlugin.native.ts` (lines 111–120)

- [ ] **Step 1: Replace the StyleURL enum values**

Find:
```typescript
export enum StyleURL {
  Street = 'mapbox://styles/mapbox/streets-v11',
  Dark = 'mapbox://styles/mapbox/dark-v10',
  Light = 'mapbox://styles/mapbox/light-v10',
  Outdoors = 'mapbox://styles/mapbox/outdoors-v11',
  Satellite = 'mapbox://styles/mapbox/satellite-v9',
  SatelliteStreet = 'mapbox://styles/mapbox/satellite-streets-v11',
  TrafficDay = 'mapbox://styles/mapbox/navigation-preview-day-v4',
  TrafficNight = 'mapbox://styles/mapbox/navigation-preview-night-v4',
}
```

Replace with:
```typescript
export enum StyleURL {
  Street = 'vnmap://styles/vnmap/streets-v1',
  Dark = 'vnmap://styles/vnmap/dark-v1',
  Light = 'vnmap://styles/vnmap/light-v1',
  Outdoors = 'vnmap://styles/vnmap/outdoors-v1',
  Satellite = 'vnmap://styles/vnmap/satellite-v1',
  SatelliteStreet = 'vnmap://styles/vnmap/satellite-streets-v1',
  TrafficDay = 'vnmap://styles/vnmap/navigation-day-v1',
  TrafficNight = 'vnmap://styles/vnmap/navigation-night-v1',
}
```

- [ ] **Step 2: Run typecheck to verify no TypeScript errors**

```powershell
cd C:\Nca\vnmapplugin
yarn typecheck
```

Expected: Exit 0, no errors.

- [ ] **Step 3: Commit**

```bash
git add src/vmapPlugin.native.ts
git commit -m "feat: update StyleURL enum to use VnMapPlugin tile server URLs (placeholders)"
```

---

### Task 12: Fix `CONTRIBUTING.md` typo and verify `RELEASE.md`

**Files:**
- Modify: `CONTRIBUTING.md`
- Verify: `RELEASE.md`

- [ ] **Step 1: Fix "exmaples" typo in CONTRIBUTING.md**

Find:
```
5.) `<doc-site>/docs/exmaples` Generate example docs (into the docosaurus site)
```
Replace with:
```
5.) `<doc-site>/docs/examples` Generate example docs (into the docosaurus site)
```

- [ ] **Step 2: Verify RELEASE.md links were updated by global replace**

```powershell
Get-Content "C:\Nca\vnmapplugin\RELEASE.md"
```

All links should reference `tommyannguyen/vnmapplugin`. If any still say `rnmapbox`, fix them manually.

- [ ] **Step 3: Verify CONTRIBUTING.md has no remaining old package references**

```powershell
Select-String -Path "C:\Nca\vnmapplugin\CONTRIBUTING.md" `
  -Pattern "rnmapbox|@rnmapbox|exmaples"
```

Expected: No output.

- [ ] **Step 4: Commit**

```bash
git add CONTRIBUTING.md RELEASE.md
git commit -m "fix: correct typo in CONTRIBUTING.md and verify RELEASE.md branding"
```

---

### Task 13: Fix autogenerated doc template `scripts/templates/component.md.ejs`

**Files:**
- Modify: `scripts/templates/component.md.ejs` (lines 6, 13, 93)

After Tasks 3 and 4, the EJS template already has:
- Line 6: `https://github.com/tommyannguyen/vnmapplugin/blob/main/...` ✓
- Line 18: `'@vnmapplugin/maps'` ✓

Two lines still say `Mapbox spec:` (linking to docs.mapbox.com style spec). Change these to `Style spec:` since VnMapPlugin doesn't have its own equivalent docs yet.

- [ ] **Step 1: Fix line 13 — component Mapbox spec link label**

Find (around line 13):
```ejs
  Mapbox spec: [<%- component.mbx.name %>](https://docs.mapbox.com/mapbox-gl-js/style-spec/layers/#<%- component.mbx.name %>)
```
Replace with:
```ejs
  Style spec: [<%- component.mbx.name %>](https://docs.mapbox.com/mapbox-gl-js/style-spec/layers/#<%- component.mbx.name %>)
```

- [ ] **Step 2: Fix line 93 — style Mapbox spec link label**

Find (around line 93):
```ejs
Mapbox spec: [<%- style.mbx.name %>](https://docs.mapbox.com/style-spec/reference/layers/#<%- style.mbx.fullName %>)
```
Replace with:
```ejs
Style spec: [<%- style.mbx.name %>](https://docs.mapbox.com/style-spec/reference/layers/#<%- style.mbx.fullName %>)
```

- [ ] **Step 3: Verify the template has no remaining old branding**

```powershell
Select-String -Path "C:\Nca\vnmapplugin\scripts\templates\component.md.ejs" `
  -Pattern "rnmapbox|@rnmapbox|Mapbox spec"
```

Expected: No output.

- [ ] **Step 4: Commit**

```bash
git add scripts/templates/component.md.ejs
git commit -m "fix: update component.md.ejs template — replace Mapbox spec labels with Style spec"
```

---

### Task 14: Final grep review pass

**Goal:** Catch any remaining old-branding instances not covered by Tasks 1–13.

- [ ] **Step 1: Grep for `rnmapbox` (lowercase)**

```powershell
Select-String -Path "C:\Nca\vnmapplugin" -Pattern "rnmapbox" -Recurse `
  -Include "*.md","*.ts","*.tsx","*.js","*.mjs","*.ejs","*.kt","*.gradle","*.rb","*.yml","*.yaml","*.json" |
  Where-Object { $_.Path -notmatch 'node_modules|\\lib\\|\.git|\.yarn|packed-refs' } |
  Where-Object { $_.Line -notmatch 'com\.rnmapbox\.rnmbx' }
```

Expected: No output. (`com.rnmapbox.rnmbx` in Java package names is the only acceptable occurrence and is filtered out above.)

If any unexpected results appear: edit each file to apply the correct replacement.

- [ ] **Step 2: Grep for `mapbox` in non-URL .md contexts**

```powershell
Select-String -Path "C:\Nca\vnmapplugin" -Pattern "mapbox" -Recurse -Include "*.md" |
  Where-Object { $_.Path -notmatch 'node_modules|\\lib\\|\.git|\.yarn' } |
  Where-Object { $_.Line -notmatch 'docs\.mapbox\.com|vnmap://styles' }
```

Expected: No output. (References to `docs.mapbox.com` in style-spec links are acceptable.)

- [ ] **Step 3: Grep for `Mapbox` in TypeScript source files**

```powershell
Select-String -Path "C:\Nca\vnmapplugin\src" -Pattern "\bMapbox\b" -Recurse `
  -Include "*.ts","*.tsx"
```

Review each result. Acceptable occurrences: internal type names (`MapboxGLEvent`, `MapboxStyles`) that are not exported in the public API. Fix any user-facing strings that still say `Mapbox`.

- [ ] **Step 4: Fix any remaining instances found in Steps 1–3**

For each file with remaining issues, apply the correct edit directly.

- [ ] **Step 5: Final commit**

```bash
git add -A
git commit -m "chore: fix remaining branding instances from review pass"
```
